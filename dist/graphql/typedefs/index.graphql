# Custom Scalars
scalar Date

type Query {
  "A list of students at that have ever attended Cornerstone"
  students(
    sort: String = "grade,lastName"
    limit: Int = 100
    page: Int = 1
    filter: StudentFilter
  ): [Student!]!
  "A student queried by ID"
  student(id: ID!): Student
}
